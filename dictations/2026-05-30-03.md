# 2026-05-30-03 — Three parallel tracks, and the local loss is wrong

*Note: Max's instruction captured by support session, not a direct dictation.*

MSE prediction loss is garbage. The key open question is: what should the local learning signal be at each block?

There should be three tracks running in parallel:

1. **Theory/research** — investigate what the local learning signal should actually be. Look at biological local learning rules, predictive coding, contrastive Hebbian learning, communication through coherence, phase-amplitude coupling. The question is: what signal makes each block learn something useful and *different* from its neighbors? Where does the current architecture diverge from how brains actually work? Don't assume Max's biases are correct — find where he's wrong.

2. **Big sets of small experiments (ablations)** — fast sanity-check-scale experiments testing different local loss variants, different lateral coupling schemes, different things the blocks could be optimizing for. Not full training runs. 30-second probes.

3. **Training run with current best architecture** — keep one long run going with whatever the current best configuration is (scaled up to use VRAM), so we have a baseline that's always improving while theory and ablations explore.

All three should be happening concurrently. Theory informs ablations, ablations validate or reject theory, best ablation result feeds into the training run.
