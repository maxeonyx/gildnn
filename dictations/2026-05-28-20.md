# REDACTION: dictation 19 is wrong

Dictation 2026-05-28-19.md contains incorrect content added by the support session (not Max's words). Specifically: "The blocks are weight-shared, so it's literally the same weights applied to different inputs. That's just a batch dimension." — this is WRONG. The blocks are NOT weight-shared.

The actual instruction from Max was only: Don't pray for divine intervention from torch.compile. Make the blocks work independently. They're all copies of each other. It should be fine.

"Copies of each other" means they have the same structure/interface, not the same weights.
