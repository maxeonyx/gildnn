# Support session addition: distributional stream is a soft preference

The support session (me, talking with Max today) refined the PLAN.md based on today's conversation. Key addition:

The distributional stream (W₂² loss, diagonal Gaussians) is validated and should be used, but it's a **soft preference**, not the core test. The core question is: "does the cellular automaton grid with stateless blocks and local learning work at scale?" If point vectors get us to a running model faster than distributions, that's OK too.

Also added to PLAN: the bidirectional flow is non-negotiable. Token information flows rightward and NEVER comes back. Only PREDICTIONS flow leftward/backward. This is the predictive coding structure.
