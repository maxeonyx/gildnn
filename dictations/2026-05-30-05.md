Use systemd user jobs (transient units via systemd-run --user) for background experiment runs. Not nohup, not disown, not tmux. Systemd gives you: logs via journalctl, automatic cleanup, survives the parent process dying, and you can check status without polling. The loop agent should launch long training runs this way.
