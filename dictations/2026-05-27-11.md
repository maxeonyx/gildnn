# Dictation 2026-05-27-11

The stream carries the actual at step 1. But then it carries the actual t1 + prediction (based on t0). Maybe. The stream accumulates — it's not just raw actuals passing through. At t=2 the stream state is something like combine(actual_t1, prediction_based_on_t0). The stream gets richer over time as predictions get folded in.

So the combining function IS important — the piece test that found "pass-through actual is the only stable option" was testing the wrong framing. It was asking "what replaces the stream after comparison?" when the real question is "what does the stream become when you fold in a prediction?"

This brings it back toward Bayesian for the combining function (prior x likelihood -> posterior), operating on distribution parameters.
