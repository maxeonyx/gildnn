# Dictation 2026-05-22-4

The loop needs to be a child of the task scheduler, not a child of the Windows terminal. Otherwise the loop gets killed when the terminal closes. Do not revert the loop.ps1 rewrite that uses Task Scheduler — that rewrite is necessary.
