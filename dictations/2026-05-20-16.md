# Broadcast mechanism: stale reads, async writes, attention, and reward modelling

Also on the broadcast mechanism — it used something like mean pooling. This is what I'm thinking about when I think about the broadcast mechanism.

Well, it should be — first of all — stale reading from all modules. If it's not doing that it's going to hurt throughput 100% because it needs to synchronize all modules which would otherwise be unsynchronized. So that's the first thing. Needs to stale read from everything, and also needs to async write back to those — they need to stale read from its output. So that's the first thing, otherwise I don't see any point in having a broadcast mechanism.

So then the broadcast mechanism I'm imagining is something like — let's say we have an auxiliary loss, like a reinforcement learning signal, that's designed to change the distribution of the outputs from the data distribution to a different distribution. Then the broadcast mechanism would be the thing that influences that. Actually, this is an interesting idea. This is a very interesting idea. Alright. So, bear with me.

We've got a predictive model. It's designed to predict its inputs from time step A to time step B. Right? But it's not designed to predict some kind of good behavior. Potentially, we should make a second predictive head, which is also tasked with predicting outputs, but constrained to some kind of second distribution of data. That is our supervised set or our reinforcement learned set. And by default, in unsupervised learning, that would be targeting the original distribution. But in supervised or reinforcement learning settings, it would be trying to predict a different distribution conditioned by something, basically.

Yeah, also the idea of the broadcast in my head was that it would be attention over all of the modules and then it would broadcast essentially the result of that attention to all modules. So it basically could take the outputs of module A and broadcast it to all modules, or it could take the submix of the outputs of module B and C and it could broadcast it to all modules. This is how two modules far away from each other might get information from each other, but only information that's useful to all modules. How the broadcast mechanism itself learns what is important or not, I don't know. Maybe self-prediction. Self-prediction by itself can't be enough, though. I'm not 100% sure how that should work. I don't think that in the human brain the attention is purely predictive. I think that is influenced a lot by the reinforcement learning part, the reward modelling. So if there's some kind of reward model I think we add in the broadcast. Without it, I think we try without the broadcast. That's my guess. Because it's probably can't learn to do anything useful with local learning.

Also, I'm concerned that we're not doing enough analysis, enough exploration of the theoretical ideas before we commit to experiments. If we are doing that exploration, I want to see it in the reports. In fact, it should be like the majority of the report, probably. And if we didn't pursue an avenue, it should be in the report too. You know — what is not the correct idea to do, and why? I want to see the mapping out of my intuitions, turning them into something more concrete and operationalized. And I want to be led through that process. I want to understand what's going on.
