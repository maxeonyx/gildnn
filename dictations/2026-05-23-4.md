# Dictation 2026-05-23-4

Context: Max reviewing the loop agent's progress since yesterday. Agent reported literature review with 30+ papers backing the project directions, matched-FLOP results, compute frontier sweep, width scaling, CUDA graph training, and longer training results.

## Literature review

> This is good. Are there any, however, that are against my project directions? I'd also be interested to just chat with you about that.

## Baselines / matched-FLOP comparison

Parallel multi-rate wins at matched FLOP versus what? Versus a transformer? If it's a language modelling task, we should be comparing versus like two controls. One is the control without our latest modification, the most similar possible network, that's one of our own architectures probably, and then also comparing to some kind of baseline model, usually a transformer because that's the accepted state of the art architecture on this same data set and task. No matter what the task is, we need to have a baseline.

## Compute frontier

I'm kind of confused as to what the compute frontier means here. Parallel wins at low compute. Okay. Sequential dominates at high compute. Weird. This is slightly weird to me, I don't really understand. Wins in terms of what? Like wall clock performance or accuracy performance?

## Width scaling

When I said scale the width in my dictation before which I actually don't know if it's seen, when I say width scaling I'm imagining like many more parallel blocks rather than just four blocks. Unless that's the actual outcome of an experiment that I haven't been made aware of, that four blocks is just the best for my GPU. I find that hard to believe, but maybe.

## CUDA graphs vs async

The CUDA graph training is great, but I've seen a line in one of the analyses that says the CUDA graph training makes the async pointless. I highly doubt that's true. The CUDA graph training is a speedup by using native code over Python, by using fused kernels or whatever over like eager training. PyTorch is notoriously slow because you have to go back and forth between the GPU all the time. Using a graph is obvious to me. I was always doing that in TensorFlow when I was doing my masters. And yeah, 10.8 times speedup is completely consistent with what I understand from that. But that's a performance we could have got with any architecture, simply by changing the training. It's completely worth it, but it doesn't make the async direction not an idea as well. In fact, it's required to even test the async idea. So it really doesn't make sense to discard the async idea after getting that speedup. I'm expecting async to have massive performance improvements at let's say like a hundred blocks. I don't know if that's reasonable or not.

## Training workflow

Longer training and larger datasets would be appreciated, but longer training is kind of annoying unless the model is actually doing other things at the same time. For example, a great workflow would be one big training run running and then a bunch of small training runs running at the same time. I believe that's possible. Currently we've got some kind of lock. That lock should be like large and small lock maybe. Or maybe yeah we can do a bunch of tiny training runs on the CPU while the GPU is going.
