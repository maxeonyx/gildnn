# 2026-05-22-11 — Async direction is NOT dead, agent tested the wrong thing

The loop agent declared the async direction "dead" based on testing PyTorch's high-level stream API. That's completely not appropriate to have discarded this angle. My imagination of this is literally not to use a high-level PyTorch API, but to be writing a CUDA kernel or something like that, specifically for this reason.

The experiment found that PyTorch streams don't give overlap — fine. But that's the easy path, not the only path. The whole point was always that we might need to go lower-level to get true parallel unsynchronised execution on the GPU.

I find it extremely hard to believe that it's not possible to run two things on the GPU literally in parallel but unsynchronised from each other. Is there not the ability to run multiple GPU programs? Do they get fully interleaved? Do some GPU units get used for different things at the same time?

If it really is the case that this isn't possible, I need a strong explanation of why — with good references, good explainer, citations to NVIDIA docs or papers. The current write-up has NO external references, it's purely the agent's own experiment with the high-level API. That's not sufficient to close this direction.

Things that should be investigated properly:
- Custom CUDA kernels that intentionally use a subset of SMs
- Persistent kernels / cooperative groups
- NVIDIA MPS (Multi-Process Service)
- What NVIDIA's own programming guide says about concurrent kernel execution
- Whether the "all SMs used by cuBLAS" claim holds for custom kernels
- What pipeline parallelism frameworks (Megatron-LM etc.) actually do at the CUDA level

Agent context: "The agent tested the wrong abstraction level. The report should be retracted or at minimum heavily qualified — scoped to 'PyTorch stream API on standard cuBLAS kernels' not 'async execution in general'. The direction remains open."
