# 2026-05-22-11 — Async direction is NOT dead, agent tested the wrong thing

The loop agent declared the async direction "dead" based on testing PyTorch's high-level stream API. This is completely not appropriate to have discarded this angle. My imagination of this is literally not to use a high-level PyTorch API, but to be writing a CUDA kernel or something like that, specifically for this reason.

I find it extremely hard to believe that it's not possible to run two things on the GPU literally in parallel but unsynchronised from each other. Is there not the ability to run multiple GPU programs? Do they get fully interleaved? Or do some GPU units get used for different things at the same time?

If it really is the case that this isn't possible, I need a strong explanation of why — with good references, good explainer, these kind of things. The current write-up has no external references, it's purely the agent's own experiment with the high-level API. That's not sufficient to close this direction.
