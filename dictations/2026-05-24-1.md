# Dictation 2026-05-24-1

## Context

Source: Google Keep note titled "Model notes", labeled `llm`. Undated, but predates this project. This is Max's original architecture vision — the note that the gildnn project is attempting to operationalize. Retrieved 2026-05-24 from Keep at Max's request.

Max's words below are unedited voice-to-text from the original note.

---

## Model notes

attention-based routing in a parallel

High parameter parallel model

Essentially my model design in mind is to have many many separate small recurrent networks that each process the own inputs and try to predict their next output at the previous time step ultimately the ones immediately adjacent in a graph to the raw inputs need to predict them and they are able to use the outputs of the owners in order to do so the ones further away in the graph need to protect the outputs of the neighbors using the outputs of their neighbors at the previous time step. Now how do inputs get aggregated by a node in the scrap well they use attention that's my idea and furthermore attention that attention would then block the computation we would have to run an attention phase and then a feed forward phase or recurrent phase but instead here's what we do we compute the attention in parallel and the attention is supposed to predict and attention for the current timestep but also an attention for the next time step in training we use the former but in inference we use the latter and the latter is trained to predict the former at the next time it's time step. I also like the idea of in general having these small predictive modules so another one would be a lot a prediction head that is designed to predict the loss of another prediction head why because if we can predict the loss then we can decide for instance time with it or not to for example stop or continue looping. For another way to use the loss is let's say we want to do predictive numbers of tokens in one timestep we can decide how many tokens to predict based on the output of our last bit which predicts a loss for a given number of tokens which train in that capability and inference time we can dynamically select how many tokens we want to sample without having to separately train that. Additional prediction heads are relatively cheap, as is an additional batch size for an tension head although the loss for that is unclear to me right now.

I'm now thinking about how I can operationalise this vision what are the end to intests that I can and metrics that I can have that would specify why the capabilities of the network I'm talking about here are necessary. What are the designs am I missing inside the space because I'm anchored on my solution, what additionally why is it uniquely capable and what end to end capability would produce this design? For example can I make a test that would say given a baseline network, which can only sample one token at a time can we train another network that can sample much much faster with less rollouts. Additionally can we do the reverse which is given another network and we put it much more additional computing time by looping into single tokens. And can we do so dynamically at inference time. If we want to do that dynamically inference time we have some metric by which we decide how to remove and I have a hypothesis that that design is to predict the loss. In butt cases predicting the loss over a set of possibilities the forest how many loops we want to do in the second how many tokens we want to sample. Ideally these would be orthogonal. For the parallel time prediction model with the graph can we make a specification for that for example given a certain number of parameters how can we minimize the number of loads in and out of memory basically can we sit all of the parameters in the gpus memory at once, and then simply parallel compute from there? Nodes further away from the inputs would be able to run less frequently I think. But those ones we might be able to load in and out more, but for the ones immediately adjacent to the inputs we might not want to load those in and out ever, at least that's my hypothesis. If we can sit as many of the parameters in memory as possible and also if we can have somehow desynchronized / unsynchronized communication between the nodes then we can do extremely fast recurrent inference where we simply process in place. We have an output storage and we use that as the attention input from neighbours that output storage would only update on the next run of the neighbor whenever that is. Ideally in training we can detach the gradients from each other - but we need some way of somehow grounding both on the input needs to be grounded as the ultimate prediction target and on the desired output. For example can we back propagate only through neighbours in the graph? Stop gradients in a overlapping fashion? It's implied that our neighbour propagation it's also time propagation, modules are probably going to be recurrent and therefore will have a single step of time propagation as well within one node. The predictive head also has neighbours. It needs to produce outputs that match the desired sequence somehow it has a reinforcement learning signal not just predicting the inputs but producing some kind of output. Perhaps we train our model in two settings, appear predictive setting and a skewed reward driven setting setting. Play reward driven setting the reward module is trained to produce outputs that feed into the graph somehow as conditioning that changes the output hits to not predict exactly the inputs but rather a skewed distribution that matches the reward setting's distribution. In neural networks the input and output modality is often the same but this one ideally would not have to be the same. It's an interesting question to how that works I think a closed loop is necessary somehow the outputs need to be observed by the inputs. Training reward models in this way is them an interesting challenge we have a predictive model over multiple modalities then we train an additional reward model to guide outputs there's outputs as they are produced can also conditionalities to predict the resulting extent inputs given outputs that will be produced. The ordinary predictive processing goals should not conflict with their additionally being a reward conditioning.
