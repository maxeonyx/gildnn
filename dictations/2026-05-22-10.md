# Dictation 2026-05-22-10

## Discussion: interpreting the fixed multi-rate experiment results

**Max:** Better validation loss for fewer compute steps from multi-rate async is pretty cool. Getting slower results means we've got stuff to work on, but this proves some kind of inductive bias that I have intuitively — that basically multi-rate means that a certain part of the network is attempting to predict further in the future.

**Max:** I don't really understand the architecture though. The blocks are in series and some are being skipped. It's a bit of a strange architecture frankly. I guess if they are all going on the same residual stream, it makes sense. Are they predicting from input time t to input time t+n where n is their rate? Or are they just running less frequently?

"Agent: They're just running less frequently. Every timestep, every block does a MixAdd into the stream — but some blocks mix in a stale cached output instead of recomputing. The rate-4 block computes on steps 0, 4, 8, 12... and on other steps it re-mixes its old output. There's no explicit prediction target. The implicit inductive bias is: that block's output has to be generic enough to be useful across 4 steps. But there's no explicit loss telling it to predict ahead — it's purely emergent from the training signal (final token prediction) plus the architectural constraint of only updating every 4th step."

**Max:** Right. So if the gradient is propagating from time t+4 to time t, then the block's output has to be generic enough. That is reasonable, I think. I think that is quite reasonable.

"Agent: The gradient from the loss at the final step propagates back through all the MixAdds, and for the rate-4 block it passes through 4 steps of 'same cached output mixed into an evolving stream.' The training signal effectively says: whatever you produced at step 0, it needed to be useful at steps 0, 1, 2, and 3. That's a real pressure toward producing something that captures a longer-timescale feature. And the fact that it got better val loss suggests that pressure is actually a useful regularizer — similar to how the sequential bottleneck was the useful thing in the earlier predictive chain experiments. Constraining how often something updates forces it to learn more slowly-varying, more generalizable representations."
