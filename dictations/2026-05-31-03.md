Take a holistic view of the whole project objectives. Explicitly go back and read the vision and original project objectives. I am not attached to a specific prediction architecture, but I have a vision for local computation.

We can have block 0 predict its next timestep inputs (NOT just next token embedding — including info propagation and also there's multiple timesteps within a token). Then we can have a global head (the attention) do the actual CE loss. That global module should still be detached.

This is the architecture: local prediction of next-timestep inputs at block 0 (which includes lateral propagation, not just the token embedding), and a detached attention head for the actual token CE loss. The local objective is predicting what your inputs will be at the next timestep — that's genuinely local, doesn't require a global signal, and is biologically plausible.
