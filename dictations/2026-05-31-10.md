Laterals flowing upward only is a bug, not a design choice. Higher levels should be able to influence lower levels — otherwise what's the point of the hierarchy? Fix this. It's probably a one-line fix (make arrivals flow both directions). Then rerun the multi-level experiments.

This might explain why multi-level was never helping CE — it literally couldn't, because information from higher levels never reached level 0 where the CE is measured.
