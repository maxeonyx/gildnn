From your notes: 

> **Finding: More aggressive dilation is a wash.** [1,2,4,16] is essentially identical to [1,2,4,8] (0.0003 difference, well within seed variance). [1,2,4,32] is slightly worse (0.004). The compute savings are marginal (122K→117K, ~5%).
> 
> This makes sense: with ctx=32, rate=32 means the slow block fires exactly once at t=0. It's a static prior, not a useful slow feature extractor.

ctx=32 still?! that's not really the point of this architecture - I thought we'd be onto *thousands* of tokens of context by this point, maybe? Obviously rate=32 at ctx=32 is not going to be useful. but rate=1024 at ctx=400000 is probably going to be useful. Obviously we can't reach extremely long context like that for now. But I would like to try, once we have good training in place, to try do some actually large datasets & runs.