# Dictation 2026-05-24-3

## Hierarchical dynamic autoregressive prediction

Okay, so nothing on a hierarchical layers of autoregressive predictors. What I'm basically imagining is: let's say we have a lower level, lowest level byte level or character level predicting autoregressive model. Right? Then, let's say it predicts some number of tokens into the future by unrolling it, and we can control its output via a conditional input, and then that conditional input is trained to also be a it's trained to be a reconstruction. So, yeah, so basically, like let's say we take one token, we put it through an autoencoder, and then we reproduce it through and it has a bottleneck. That bottleneck is a representation of the token. But then we take two tokens, we put it through a bottleneck, and then we unroll autoregressively from the embedding to produce two tokens. Now, that is a pro that is a that bottleneck is a representation of two tokens. So, now we train an compressor and decompressor to do as many tokens as we can. But we do our loss prediction thing from earlier in the project so that we know where to stop. And then, instead of continuing with that low-level auto-encoder and a bottleneck, we make another one for the remaining tokens, right? And then we have all these bottlenecks in sequence, each which predict maybe one token, maybe eight tokens. It's a dynamic tokenization basically with autoregressive unrolling. Then, we have a sequence of these higher-level chunks, bottlenecks. Then, we do autoregressive prediction over those. Now we can continue this up multiple layers. So we have a character level autoregression, a token level autoregression, a phrase level autoregression, a bad blah, and we go up like number of orders of magnitude, maybe we'll eventually get to something quite high level. This is kind of what I mean by dynamic hierarchical. So there's basically two things going on as well. First of all, there is a auto there is a autoregressive autoencoder that is trained for reconstruction. And then there is a next token prediction goal. The autoregressive the sorry the we want the latent not to be able to just u useful for decod for reconstruction but also useful for next token prediction. So it's going to be useful for the next autoregressive unit. It's going to be useful for that. So we train two in parall at two with each other. And in training we maybe kind of overlap them, I don't know. But then in inference we we don't have to overlap them.
