completely agree with "predict k time steps ahead" - do it, but i wonder if theres also a more dynamic version of it, related to accumulating "suprise" or something like that, and only teiggering if the input exceeds some kind of theshold. not clear. do the naive thing here. also, i dont underatand why we arent on huge endless sequences. this arch has no sequence length constraint. id probably split the dataset into batch size number of sequences, say 32 or w/e, then treat all tokens as one massive sequence.
