I think you've maybe not been understanding me what the diagonal here means. I'll try to articulate it a bit more clearly. Essentially, let's say we run a number of blocks in parallel. They all take their inputs from a set of inputs independently, and then they produce a set of outputs. Then, at the next time step, they are able to take as input the outputs of their neighbors immediately adjacent. That means that the information propagates across through the graph at one node per time step. That's what I mean by a diagonal here. We run a bunch of things in parallel and the time steps are also the information propagation through the stack. It's horizontal kind of. Not a deep network, but a wide network where well yeah anyways, it's a it's a deep network where you run all of the depth in parallel and then you run it again and you run it again. And information only goes down one step per time step. As far as I know, you haven't run an experiment qu quite like that yet. Correct me if I'm wrong. 