Clarification on the queueing mechanism from dictation 05: it should auto-launch the next experiment when one finishes, so the loop doesn't need to know when something is done. Two queues — an ablation track and a big run track. The loop fills the queues, the queues drain themselves, and the loop reads results whenever it gets around to it.
