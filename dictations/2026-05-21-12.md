# Dictation 2026-05-21-12

The specific mechanism I want is: the experiment sub-agent launches the task, but then doesn't poll. It says it's done for now, it has launched the task. The parent agent should be told to get the sub-agent to launch the task only, then stop. The parent agent will be in charge of following up on that experiment — it will resume the sub-agent later when it wants to check on the run.
