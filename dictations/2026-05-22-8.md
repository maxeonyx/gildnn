# Dictation 2026-05-22-8

On the plan structure: the plan should have two levels. The top level is the overall research project — what's the architecture, what are its pieces, what experiments do we need to run, what order does it make sense to go in. VISION.md should serve this purpose. The bottom level is the immediate checklist — what's the specific next experiment, what will I do based on each outcome, the live sequence. PLAN.md should serve this purpose.

On the vision: VISION.md is probably a bit stale. There are bits missing and bits to add — the async wall clock speedup as the core goal, the diagonal residual connections, multi-rate execution, self-prediction, dynamic token count output. The agent should do a one-time rewrite of VISION.md from the dictations. Keep my language as much as possible but rearrange it to make it flow more cleanly. This should be a planned task, done once, then ticked off. Not something that happens on every handover.

On handovers and reorientation: when an agent receives a handover or the reorientation prompt, it should be told to read VISION.md to get up to speed on the overall research project. They'll only be doing one piece of it right now, and that's fine. But they should know where that piece sits in the bigger picture.

Put an item in PLAN.md to do the VISION.md rewrite once from the dictations, then tick it off. When doing the rewrite, do multiple rounds of review — explicitly asking each time: does this capture "the spirit of my vision"? Use that exact phrase. The rewrite should not just aggregate my words, it should reflect the spirit of what I'm trying to do.
