# Fused kernels, any method, no silent fallback

We want fused kernels — the whole block computation in one kernel, data stays on GPU between ops. Triton is one way, but I'm ok with any method so long as it doesn't silently fall back to eager/CPU dispatch. If it can't fuse, it should error, not quietly degrade.
