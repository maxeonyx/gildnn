# No more slow experiments

Do NOT run more slow experiments on the Python-loop automaton. It's the last day. The whole point of dictations 18-25 is that the implementation needs to be GPU-native (fused kernels, no CPU fallback). Running 4-hour ablations on the slow version is wasted time. Read dictations 18-25 and work on the GPU-native implementation.
