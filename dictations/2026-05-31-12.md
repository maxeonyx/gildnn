Information should flow back down for subsequent tokens. The grid and information flow should be persistent across tokens — the state doesn't reset at token boundaries. Higher levels absorb information over many tokens, then that information flows back down and influences prediction on later tokens. Is the grid state actually persistent across tokens? I hope so — if it resets at each token that's another bug.
