# Week ending 2026-05-30 — It learns. The project ends here.

A month ago: "does any piece of this work?" Now: 192-module 2D graph cellular automaton, 1.48 nats learned in 1000 steps, per-module InfoNCE local loss, detached laterals, 8 multi-rate bands with measurable differentiation, fused Triton forward kernel at 1.94x. A 5000-step long run is in progress as I write this.

---

## What this week proved

**The full architecture learns.** 8 bands × 24 positions, d_stream=96, d_hidden=384, 16M parameters. InfoNCE local loss, detached laterals, refractory messaging. CE 4.17→2.69 in 1000 steps — 1.48 nats learned from a log(64)≈4.16 random baseline (`runs/graph_infonce_1000_clipped.ignore.log`).

**Multi-rate structure creates measurable differentiation in local prediction.** Band performance at step 1000:

| Band | Rate (steps/fire) | Local loss (nats) |
|------|-------------------|-------------------|
| 1 | 2 | 1.65 |
| 2 | 4 | 1.78 |
| 3 | 8 | 1.87 |
| 4 | 16 | 1.38 |
| 5 | 32 | 1.38 |
| 6 | 64 | 2.60 |
| 7 | 128 | 3.52 |

Inverted U. Mid-rate bands learn strongest — they hit the sweet spot between "enough training events" and "targets that carry real information." Bands 4-5 accumulate ~60-120 fire events per 1000 inner steps, enough to train but rare enough that the lateral signal has genuinely changed between fires.

**But: no temporal specialization yet.** A lag-spectrum linear probe (predict "what character was k tokens ago?" from each band's emitted representation) shows bands 2-7 are indistinguishable from random at step 1000. Only band 0 improved (22.9%→31.3% for lag=1). The per-band InfoNCE losses went down, but the representations themselves haven't differentiated — only the prediction heads got better at their local contrastive task.

```text
Lag probe, trained step 1000 vs random:
       lag=  1 lag=  2 lag=  4 lag=  8 lag= 16 lag= 32 lag= 64
band0:  31.3%  23.5%  19.6%  16.0%  13.6%  13.6%  13.8%  (random: 22.9%)
band1:  17.4%  16.1%  16.3%  15.4%  14.9%  14.4%  15.0%  (random: 14.5%)
band2:  14.6%  14.8%  15.3%  14.7%  14.3%  15.0%  15.5%  (random: 14.7%)
band5:  14.6%  15.8%  12.9%  13.2%  13.1%  12.4%  12.4%  (random: 14.2%)
```

This means: at 1000 steps, the CE improvement comes entirely from band 0 learning better token prediction. Higher bands are predicting their local neighbors' outputs correctly (InfoNCE loss below random), but haven't developed representations that encode anything about the input character sequence. The 5000-step run tests whether this changes with more training — it's a bootstrap: band 0 must first produce useful representations, then band 1 must learn from them, then the benefit must propagate back.

**InfoNCE beats MSE as local loss.** CE 2.41 vs 2.53 at step 19 — 0.12 nats (`runs/ablation_infonce.ignore.log`). InfoNCE gives the predictor a contrastive signal against negatives rather than asking it to reconstruct a high-dimensional target directly.

**Refractory improves CE despite making prediction harder.** 2.46 vs 2.55 baseline (`runs/ablation_refractory.ignore.log`). Preventing a module from sending to a neighbor it just received from forces information to take longer paths — more diverse routing, better transport.

**Triton fused forward: 1.94x.** 913ms vs 1772ms at batch=16 (`core/triton_forward.py`). One kernel per module per timestep. Reads neighbor states from global memory without block synchronization. The backward kernel design is identified but exceeds remaining time.

---

## What didn't work

**torch.compile hangs indefinitely** on this model — the 1024-iteration Python loop is too complex for the Dynamo tracer. **CUDA graphs fail** because of CPU↔CUDA copy during graph capture. Custom Triton is the only viable acceleration path.

**Triton backward was scoped but not built.** The correct design (per-module local gradient, no cross-module backward pass needed because laterals are detached) is simple in principle. The implementation — atomic accumulation into neighbor gradient buffers — is a full day of work that didn't fit.

---

## The month

Environment → piece tests → local learning discovery → backend decision → linear automaton → 2D graph with Triton. Each week resolved the blockers that made the next week's work interpretable. The key discovery was Week 3: detached laterals aren't a parallelism compromise, they're strictly better (3.85 vs 4.07 nats, gap growing). That one result made the entire cellular automaton design viable — you don't need global backprop through the graph.

---

## Honest assessment

The architecture learns. But "learns" means "band 0 predicts tokens; higher bands predict their neighbors' outputs." Whether the multi-rate structure creates genuine functional specialization — different bands encoding different temporal spans, feeding back to improve overall prediction — is unresolved at 1000 steps.

The lag probe is the key discriminator. At 1000 steps: no specialization. The 5000-step run (underway, step ~1070 as of writing) is the test. If bands 2-7 remain indistinguishable from random in the lag probe at step 5000, the architecture is doing local self-prediction without developing the cascading abstraction it was designed for. If they differentiate, the bootstrap worked — it just needed more time.

What's missing for a stronger claim: the probe at step 5000, and ideally an ablation showing that band 0's CE benefits from reading trained higher-band outputs vs reading random-weight outputs.

---

## What a future session picks up

The codebase is `core/` with the graph automaton model, Triton forward kernel, InfoNCE loss, training loop, and two diagnostic probes (impulse response + lag spectrum). A 5000-step run is in progress.

**Immediate next steps:**
1. When 5000-step run completes: re-run lag probe on step_005000 checkpoint — does specialization emerge?
2. Ablation: zero out higher-band messages to band 0 — does CE degrade? (Would prove higher bands contribute)
3. If no specialization at 5000 steps: consider whether bidirectional prediction flow (higher bands sending predictions back) is necessary for the bootstrap
4. Triton backward kernel — the design is clean (local gradients, atomic accumulation into neighbor buffers), worth ~1 day

**The question that matters:** does the multi-rate graph develop cascading abstraction (band k encodes timescale k information that helps band 0 predict tokens), or does it just train 192 independent local predictors that don't cooperate?
