# Week of 2026-05-08

One-day week. No research measurements yet, but the week did produce one real runtime proof: a tiny PyTorch tensor op executed on CUDA on this machine.

## The strongest artifact this week

From [`research/questions/backend-validation/README.md`](../questions/backend-validation/README.md):

```json
{
  "python_version": "3.12.12",
  "torch_version": "2.11.0+cu128",
  "torch_cuda_runtime": "12.8",
  "cuda_is_available": true,
  "cuda_device_count": 1,
  "tensor_device": "cuda:0",
  "result_device": "cuda:0",
  "result_values": [2.0, 4.0, 6.0],
  "current_device_name": "NVIDIA GeForce RTX 3090"
}
```

That is not a model result. It is better than scaffolding, though: the repo now has one checked-in operational proof that the active machine can run at least one backend on GPU.

The probe was made reproducible enough to pick up again. From [`pyproject.toml`](../../pyproject.toml):

```toml
[project]
requires-python = "==3.12.*"
dependencies = [
    "torch==2.11.0+cu128",
]
```

And the project contract records the scope correctly. From [`AGENTS.md`](../../AGENTS.md):

```md
- Verified on 2026-05-08: one minimal local backend path works on this Windows machine with UV + local `.venv`, CPython 3.12.12, and `torch==2.11.0+cu128`.
- This is an operational probe only. It does **not** settle the project-wide backend choice.
```

## Honest assessment

Zero research progress. Positive setup progress with one real machine-level proof artifact.

What remains open is now narrower. From [`PLAN.md`](../../PLAN.md):

```md
1. **Use the currently proven path for the first sanity-check experiment** — start with the proven PyTorch path, while keeping backend choice open and only splitting out another backend probe if that becomes the more discriminating next step.
2. **Sanity-check experiment** — 5-char context, feedforward network, or something like that, next-token prediction on character-level text.
```

And from [`research/questions/backend-validation/README.md`](../questions/backend-validation/README.md):

```md
- It does **not** prove the experiment harness, sanity-check pipeline, or first model.
- It does **not** prove `loop.ps1` end-to-end in production.
```

**The week's real output is now slightly better than "the docs are cleaner": one backend path is proven on CUDA, while the strategic backend choice and every actual experiment remain open.**
