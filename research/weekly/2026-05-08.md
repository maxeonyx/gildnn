# Week of 2026-05-08

One-day week. The Python restart.

## What happened

Rust codebase archived to `rust-archive/`. Scaffolding rebuilt: [`loop.ps1`](../../loop.ps1), docs, eleven question folders under `research/questions/`. No Python code. No experiments. No measurements.

The most evidential artifact is [`loop.ps1`](../../loop.ps1) (61 lines on disk). Working-directory fix and crash backoff are present and readable:

```powershell
Set-Location $PSScriptRoot
```

```powershell
$backoff = [Math]::Min(60 * $crashCount, 300)
```

The script has not been run end-to-end. Whether `opencode run --no-ephemeral=true` works as intended in production is untested.

See [daily/2026-05-08.md](../daily/2026-05-08.md) for the full account.

## The contradiction in PLAN.md

[`PLAN.md`](../../PLAN.md) holds two claims that sit in tension:

> "Project setup is complete enough to begin experiments."

> "No Python code yet." / "1. **Environment setup** — choose and validate Python backend + CUDA"

This is not a reporting error — it is the current state of the plan, honestly reproduced. "Complete enough" likely refers to scaffolding (harness, docs, question folders), not a runnable environment. But no experiment is runnable until the venv exists and a tensor confirms CUDA. The question folders register the same gap. From [`research/questions/mix-add/README.md`](../questions/mix-add/README.md):

> "Not yet investigated experimentally in this repo."

## Honest assessment

Zero research progress. Bounded, positive setup progress.

Three blockers remain before any measurement is possible:

1. Python environment — no venv, no packages, no CUDA confirmation
2. Sanity-check pipeline — not run
3. Baselines — not started

**The week's output is a more-debugged harness and coherent scaffolding. No claim about experimental readiness is supportable until the venv runs and a tensor hits CUDA.**
