# 2026-05-08

Process-only day. No experiments ran. No Python code was written.

## What changed

The Rust codebase moved to `rust-archive/`. Eleven commits across the day rebuilt the project scaffolding: `loop.ps1`, `AGENTS.md`, `PLAN.md`, `PROCESS.md`, `VISION.md`, and eleven question folders under `research/questions/`. The commit sequence is visible in `git log --oneline` — the last five entries are the Python-restart work, beginning at `d91ab6d`.

The most iterated artifact is `loop.ps1`. It went through at least six commits fixing flags (`--no-ephemeral` syntax), missing `Set-Location`, a sleep/continue contradiction, and an overly explicit prompt. The current version (61 lines, committed as `ba5ea4b`) sets working directory, passes `--session`, backs off on fast crashes, and prompts the agent to read `PLAN.md` and check for due reports. It has not been run end-to-end — the script exists and is syntactically correct, but whether the `opencode run` invocation works as intended in production is untested.

## Risk reduced

**The harness is better-specified than before.** `Set-Location $PSScriptRoot` fixes the working directory problem. The crash backoff (linear, capped at 300s) prevents a tight crash loop. These are verifiable from the file — they weren't present in earlier versions.

**The question folders exist.** Eleven folders in `research/questions/` (e.g. `mix-add/`, `dynamic-depth/`, `cortical-columns/`-adjacent names). They are empty of experimental data; their value is organizational, not evidential.

## What remains unverified

- `loop.ps1` end-to-end: no production run. The fix history is paper debugging.
- Python environment: no venv, no package installation, no CUDA check.
- No experiment pipeline exists. The sanity-check (5-char context, character-level next-token) has not run.

The question folders are scaffolding. None contain data.

## Why this was still the right step

Starting experiments without a working harness and coherent docs risks producing results that can't be reproduced or recovered after a session boundary. With a hard end-of-month timebox, a bad start is more costly than a slow one. Setup debt is cheaper before the first experiment than after.

The next discriminating step is: create the venv, install JAX or PyTorch, confirm a tensor runs on CUDA, then run the sanity-check pipeline. That result — or its failure — is what tomorrow's report should anchor on.
