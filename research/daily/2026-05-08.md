# 2026-05-08

Process-only day. No experiments ran. No Python code was written.

## What changed

The Rust codebase moved to `rust-archive/`. Project scaffolding was rebuilt: `loop.ps1`, `AGENTS.md`, `PLAN.md`, `PROCESS.md`, `VISION.md`, and eleven question folders under `research/questions/`.

The most iterated artifact is [`loop.ps1`](../../loop.ps1) — 61 lines, verifiable on disk. Key fixes embedded in the current file:

```powershell
# Ensure we're always running from the repo root, regardless of where the script was launched from
Set-Location $PSScriptRoot
```

```powershell
# Back off if it's crashing fast, to avoid a tight crash loop
if ($elapsed -lt 30) {
    $crashCount++
    $backoff = [Math]::Min(60 * $crashCount, 300)
```

The working-directory fix and linear crash backoff (capped at 300 s) are present in the file. How many revision passes it took to get here is not recoverable from the file alone — the fix history cited in earlier drafts was from git log, not from the artifact.

## The PLAN.md contradiction

[`PLAN.md`](../../PLAN.md) currently contains both of these statements:

> "Project setup is complete enough to begin experiments."

> "No Python code yet."

> "1. **Environment setup** — choose and validate Python backend + CUDA"

These are not reconciled. "Complete enough to begin" appears to mean the scaffolding (harness, docs, question folders) is in place — not that the environment is ready. But the same file lists environment setup as immediate priority 1. The reports should not resolve this; it is a genuine open question about what "complete enough" means. No experiment is runnable until the venv exists, packages are installed, and a tensor hits CUDA.

## Risk reduced

**Harness is better-specified.** `Set-Location $PSScriptRoot` fixes the working-directory problem. Crash backoff prevents a tight restart loop. Both are verifiable from the file.

**Question folders exist.** Eleven folders in `research/questions/` (e.g. [`mix-add/`](../questions/mix-add/README.md), [`dynamic-depth/`](../questions/dynamic-depth/README.md)). From [`mix-add/README.md`](../questions/mix-add/README.md):

> "Not yet investigated experimentally in this repo."

Their value is organizational, not evidential. None contain data.

## What remains unverified

- `loop.ps1` end-to-end: no production run on record.
- Python environment: no venv, no package installation, no CUDA check.
- No experiment pipeline exists. The sanity-check (5-char context, character-level next-token) has not run.

## Why this was still the right step

Starting experiments without a working harness and coherent docs risks producing results that can't be reproduced or recovered after a session boundary. With a hard end-of-month timebox, a bad start is more costly than a slow one.

The next discriminating step: create the venv, install JAX or PyTorch, confirm a tensor runs on CUDA, then run the sanity-check pipeline. That result — or its failure — is what tomorrow's report should anchor on.
