# 2026-05-08

No experiments ran today. The main concrete result was operational: one minimal local Python/CUDA path now works on this machine.

## What changed understanding

From [`research/questions/backend-validation/README.md`](../questions/backend-validation/README.md):

```json
{
  "python_version": "3.12.12",
  "torch_version": "2.11.0+cu128",
  "torch_cuda_runtime": "12.8",
  "cuda_is_available": true,
  "cuda_device_count": 1,
  "tensor_device": "cuda:0",
  "result_device": "cuda:0",
  "result_values": [2.0, 4.0, 6.0],
  "current_device_name": "NVIDIA GeForce RTX 3090"
}
```

That is enough to say a tiny PyTorch tensor op really executed on CUDA here. It is not enough to say the project backend is chosen, the harness is proven end-to-end, or the first model is ready.

The probe was pinned in checked-in project files. From [`pyproject.toml`](../../pyproject.toml):

```toml
[project]
requires-python = "==3.12.*"
dependencies = [
    "torch==2.11.0+cu128",
]
```

From [`.python-version`](../../.python-version):

```text
3.12
```

## What this did and did not settle

The verified runtime facts are now recorded in [`AGENTS.md`](../../AGENTS.md):

```md
- Verified on 2026-05-08: one minimal local backend path works on this Windows machine with UV + local `.venv`, CPython 3.12.12, and `torch==2.11.0+cu128`.
- CUDA proof for that path: tiny tensor multiply succeeded on `cuda:0` / `NVIDIA GeForce RTX 3090`.
- This is an operational probe only. It does **not** settle the project-wide backend choice.
```

That resolves the earlier "can this machine run anything at all?" question. It does **not** resolve the backend decision. PyTorch is now one proven local path; the broader backend choice remains open.

## What remains unverified

From [`research/questions/backend-validation/README.md`](../questions/backend-validation/README.md):

```md
- It does **not** prove the experiment harness, sanity-check pipeline, or first model.
- It does **not** prove `loop.ps1` end-to-end in production.
```

And from [`PLAN.md`](../../PLAN.md):

```md
1. **Use the currently proven path for the first sanity-check experiment** — start with the proven PyTorch path, while keeping backend choice open and only splitting out another backend probe if that becomes the more discriminating next step.
2. **Sanity-check experiment** — 5-char context, feedforward network, or something like that, next-token prediction on character-level text.
```

## Why this was the right step

Before today, the repo had good process scaffolding and no validated runtime path. After today, there is at least one real machine-level proof artifact. That sharply reduces the risk that the first model attempt fails for reasons that have nothing to do with the model.

The next discriminating step is now narrower than it was this morning: use the proven PyTorch path for the smallest sanity-check experiment, unless another backend probe becomes the sharper way to reduce uncertainty, without pretending the strategic backend choice is settled.
