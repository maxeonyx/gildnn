# 2026-05-25 — Tied-depth "instability" was a confounded comparison

## The confound

Overnight tied-depth experiment finished (4 variants × 2 seeds, WikiText-103 char-level). The initial read was alarming: tied_8iter diverges on seed 43, plateaus at 2.549 from step 5K onward. "Weight sharing is fundamentally unstable" was the interim narrative. The adversarial review gate killed it.

The problem: tied_8iter uses `token_injection=block0` (tokens enter once, hidden state iterates 8×). distinct_matched uses `token_injection=all` (fresh tokens every block). So the comparison conflates three things simultaneously — weight sharing, token routing, and block shape (one d=256 block vs eight d=146 blocks). You can't attribute the failure to any one of them.

On the seed where tied_8iter converges (seed 42), it *beats* distinct_matched: 1.798 vs 1.818. On the seed where it fails (seed 43), the lack of token re-injection after iteration 1 is as plausible an explanation as weight sharing itself — there's no anchoring signal to recover from early drift.

Full results:

| Variant | Seed 42 | Seed 43 | Mean | Std | Params |
|---------|---------|---------|------|-----|--------|
| A_single | 1.832 | 1.845 | 1.838 | 0.007 | 2,850,422 |
| tied_8iter | 1.798 | 2.549 | 2.173 | 0.375 | 2,850,422 |
| distinct_matched | 1.818 | 1.817 | 1.817 | 0.001 | 2,847,908 |
| distinct_rich | 1.750 | 1.744 | 1.747 | 0.003 | 4,690,820 |

Transformer baseline: 1.592 ± 0.003 (2.86M params).

Log: [`experiments/wikitext_103/artifacts/tied_depth/run.jsonl`](../../experiments/wikitext_103/artifacts/tied_depth/run.jsonl). Report: [`experiments/wikitext_103/artifacts/tied_depth/report.json`](../../experiments/wikitext_103/artifacts/tied_depth/report.json).

---

## The clean test: weight sharing isolated

To actually test Pathway 1 at this scale, both variants need identical routing. Added a `tied_weights` flag to `ParallelDiagonalModel` in `core/model.py`. The new experiment (`runs/tied_sharing.py`):

- **tied_shared:** 8 blocks, shared weights, d=146, ff=584, `token_injection=all`
- **distinct_matched:** 8 distinct blocks, same config

Same routing, same topology, same FLOPs. Only difference: whether blocks share parameters.

- tied_shared: 1,649,102 params (the reduction IS the point — sharing's benefit is parameter efficiency)
- distinct_matched: 2,847,908 params
- 3 seeds (42, 43, 44)

Sanity-checked, committed, queued behind C_old.

---

## C_old ablation (running)

C_lateral vs C_isolated — same param count, only lateral connections differ. Tests whether the multi-block-with-token-injection architecture actually uses cross-block communication or is just an ensemble.

First run (PID 12096) crashed at step 14K with no captured error (crash capture wasn't installed yet). GPU was clean on restart. Restarted as PID 20548 at 20:46; now at step 14K/20K on C_lateral seed 42, val_loss 1.865, 115K tok/s. Expected completion ~22:30-23:00.

If C_isolated ≈ C_lateral → the blocks aren't communicating, and the architecture is wasting structure. If C_lateral < C_isolated → lateral connections carry information, Pathway 3 has legs.

---

## Process note

The adversarial review gate worked as designed. "CRITICAL FINDING: fundamental stability problem" (prior session's language) was overclaiming given a confounded setup. PLAN.md and the question README now reflect the honest state: we don't yet know whether weight sharing is stable, because we haven't cleanly tested it.

---

## What's discriminating next

1. **C_old result** (hours away) — Pathway 3 viability
2. **Clean weight-sharing isolation** (queued) — the real Pathway 1 test
3. If sharing works with correct routing → validates the tied architecture → unlocks dynamic depth
