# 2026-05-22

Fixed multi-rate execution works. Not marginally — the result is clear: skip computation on a fixed schedule, reuse cached outputs, and get 15-21% real wall-clock speedup with *better* quality than running every block every step. The rate constraint acts as a regularizer, forcing blocks to produce longer-timescale representations.

## Multi-rate [1,1,2,4]: 14.8% speedup, quality better

Four blocks, rates [1, 1, 2, 4]. Rate-4 block executes every 4th timestep; on skipped steps, its cached output is MixAdd'd into the stream unchanged.

| Step | All-rate-1 val loss | Multi-rate val loss | Delta | Speedup |
|------|----:|----:|------:|------:|
| 500 | 2.188 | 2.171 | -0.017 | 11.2% |
| 1000 | 2.062 | 2.052 | -0.010 | 12.4% |

Final timing (1000 passes): **14.8%** speedup. Artifact: [`experiments/fixed_multi_rate/artifacts/short_run/report.json`](../../experiments/fixed_multi_rate/artifacts/short_run/report.json).

## Multi-rate [1,2,4,8]: 20.7% speedup, quality better

More aggressive: each successive block halves its execution frequency. Rate-8 block computes 4 times across a 32-character context.

| Step | All-rate-1 val loss | Multi-rate val loss | Delta |
|------|----:|----:|------:|
| 500 | 2.188 | 2.168 | -0.020 |
| 1000 | 2.062 | 2.051 | -0.010 |
| 2000 | 1.946 | 1.941 | -0.006 |

Final timing (500 passes): all-rate-1 73.89ms, multi-rate 58.59ms → **20.7%** speedup. Artifact: [`experiments/fixed_multi_rate/artifacts/aggressive_1248/report.json`](../../experiments/fixed_multi_rate/artifacts/aggressive_1248/report.json).

Quality advantage is consistent across all checkpoints (multi-rate always lower loss). Max's interpretation ([dictation 2026-05-22-10](../../dictations/2026-05-22-10.md)): the rate constraint pressures blocks toward "predicting further in the future" — their output must be useful across multiple timesteps.

## Why this works where selective execution failed

Selective execution (learned gating): 26-29% *slower* — the gating overhead exceeds the savings from skipping cheap blocks. Fixed multi-rate: zero decision overhead (the schedule is a compile-time constant). The full compute reduction translates directly to wall-clock savings.

## Direction change from Max

[Dictation 2026-05-22-6](../../dictations/2026-05-22-6.md): "I want to go back to the original async plan — the diagonal residual connections across time and depth, modules running at different rates, the thing that might actually give wall clock speedup."

VISION.md rewritten from all dictations per [dictation 2026-05-22-8](../../dictations/2026-05-22-8.md). Process docs updated with delegation pattern per [dictation 2026-05-22-9](../../dictations/2026-05-22-9.md).

## What's running now

Nothing — GPU occupied (gaming). Free after midnight. Launch mechanism debugged: CUDA init blocks when GPU is in exclusive use; `nvidia-smi` preflight added to process rules.

## Diagonal multi-seed confirmation: NEGATIVE

Three seeds × 2000 steps, [1,2,4,8] with diagonal vs without:

| Seed | Control | Diagonal | Delta |
|------|---:|---:|---:|
| 42 | 1.988 | 1.935 | **-0.052** |
| 43 | 1.934 | 1.934 | **+0.000** |
| 44 | 1.959 | **18.386** | **+16.4 (diverged)** |

The -0.052 from seed 42 was not a typical outcome. Seed 43 showed zero effect. Seed 44's diagonal model catastrophically diverged between step 1500 and 2000 — the control trained normally. The naive diagonal connection is **unstable and unreliable**. A gated/scaled variant (learnable mixing starting near zero) might stabilize it, but that's a new experiment.

Artifact: [`experiments/fixed_multi_rate/artifacts/diagonal_1248_multi_seed/report.json`](../../experiments/fixed_multi_rate/artifacts/diagonal_1248_multi_seed/report.json).

## Diagonal + multi-rate: suggestive but not settled

Adding diagonal connections (block A's output delta at time t feeds block B at time t+1) to multi-rate [1,2,4,8]:

| Step | Control (multi-rate) | Diagonal | Delta |
|------|--:|--:|--:|
| 500 | 2.192 | 2.169 | -0.022 |
| 1000 | 2.066 | 2.064 | -0.002 |
| 1500 | 1.983 | 1.992 | +0.009 |
| 2000 | 1.988 | 1.935 | **-0.052** |

The -0.052 at step 2000 is large, but the trajectory is non-monotonic (crosses zero at step 1500). The control's val loss worsened in the last 500 steps while diagonal kept improving — consistent with a stabilization/regularization effect, but could also be seed variance.

Not settled. Needs longer run or multi-seed to confirm. Artifact: [`experiments/fixed_multi_rate/artifacts/diagonal_1248_vs_baseline/report.json`](../../experiments/fixed_multi_rate/artifacts/diagonal_1248_vs_baseline/report.json).

## Async hardware investigation: question reopened

Per [dictation 2026-05-22-11](../../dictations/2026-05-22-11.md), the previous "decisive negative" only tested PyTorch's high-level stream API. The RTX 3090 (GA102, 82 SMs) hardware supports concurrent kernel execution — different SMs can run different kernels simultaneously. Paths forward: persistent CUDA kernels (zero launch overhead), fused blockIdx dispatch, or CUDA Graphs. See corrected [README](../questions/async-execution/README.md).

## Backend decision: PyTorch + torch.compile

Per [dictation 2026-05-22-12](../../dictations/2026-05-22-12.md): "nothing should be blocked on me — choose something." Decision: PyTorch deliberately, with `torch.compile` for stable core/ forward paths, custom CUDA/Triton for async execution research.

Why not JAX: the project's core goal (async via persistent CUDA kernels) goes BELOW both frameworks. Triton is the more natural path to custom kernels from PyTorch. JAX's advantage (strict compilation) is irrelevant for the hardest part. See [backend-choice README](../questions/backend-choice/README.md).

## Core reintegration complete

Promoted proven components to `core/model.py`: MixAdd, ResidualFeedForwardBlock, TemporalWindowAttention, MultiRateResidualModel. Forward paths are `torch.compile`-compatible (no `.item()`, no side effects). Old models (tiny_char_transformer, async_gru) moved to `legacy_models/`.

## Literature review

Per [dictation 2026-05-22-13](../../dictations/2026-05-22-13.md): 30+ papers backing our three directions. Key finding: our multi-rate result maps directly to Clockwork RNN (2014) but with a novel observation — fixed deterministic rates REGULARIZE (quality better, not just equivalent). Stochastic Depth (2016) and Mixture-of-Depths (2024) are the closest comparisons. See [`research/questions/literature-backing/README.md`](../questions/literature-backing/README.md).

## Matched-FLOP: compute-equivalent, multi-rate doesn't sacrifice quality

The key question: if you give the all-rate-1 model the same compute budget that multi-rate "saves" (by widening it until step time matches), does the wider model win?

**Answer: no.** Calibration picked d_model=112 as the compute match (252ms/step vs multi-rate's 257ms). After 2000 steps on the same data:

| Model | Params | Val loss | Δ vs multi-rate |
|-------|--------|----------|-----------------|
| Multi-rate 4×128 [1,2,4,8] | 349K | **1.927** | — |
| Matched all-rate-1 4×112 | 270K | 1.935 | +0.008 |
| Shallow all-rate-1 3×128 | 284K | 1.946 | +0.019 |

Delta: **-0.008 nats** → **tie** (within ±0.01 threshold). Multi-rate has 30% more parameters for the same compute cost — those parameters are "free" because blocks at higher rates execute less often.

Interpretation per [dictation 2026-05-22-10](../../dictations/2026-05-22-10.md): multi-rate doesn't just skip compute — it produces a different kind of representation. The rate constraint means slow blocks MUST produce outputs useful across multiple timesteps. This is architecturally interesting even without a quality win: it gives structural headroom for async execution at zero quality cost.

Seeds 43 and 44 running to confirm the tie is stable. Artifact: [`matched_flop/report.json`](../../experiments/fixed_multi_rate/artifacts/matched_flop/report.json).

## Next

- Seeds 43/44 confirmation of matched-FLOP tie
- Persistent kernel microbenchmark (experiment designed, see [async-execution README](../questions/async-execution/README.md#experiment-2-persistent-kernel-microbenchmark-planned))
- Gated diagonal variant (stabilized version of the negative result — lower priority)

## Evening session: integration and theory

GPU blocked (gaming). Used the time for:

**Core extraction:** Created `core/training.py` with `evaluate_model`, `batched_pairs`, `fixed_step_indices`, `write_json`, `current_git_sha`, `current_git_status_short`. These were duplicated across 19-62 experiment scripts. Active experiment updated to use shared versions; archived experiments left unchanged.

**Persistent kernel experiment design:** Report-first protocol for the Triton persistent kernel microbenchmark. Key finding from scale analysis: at `d_model=128` with 4 blocks, SM partitioning is probably too small to show concurrency benefit. The experiment design separates "persistent/fusion benefit" (baseline C) from "actual async concurrency" (variant D) with a 6-point shape sweep. See [async-execution README](../questions/async-execution/README.md).

**torch.compile verified:** Graph tracing works with our model (eager backend). CUDA/Triton compilation needs GPU (no MSVC for CPU fallback on this machine). Ready for benchmarking when GPU is free.

**Matched-FLOP CPU dry run:** Full pipeline works end-to-end — calibration selects d_model=112, all 3 models train, eval produces sensible metrics. Ready to launch on GPU.

**Code quality:** Adopted einops + jaxtyping in `core/model.py` — named dimension operations replace raw reshapes/permutes. Added `core/check_dictations.py` for automatic detection of new dictation files. Created `loop-status.ps1` for quick visibility into loop state. Added Alice in Wonderland and War & Peace corpora for cross-validation.
