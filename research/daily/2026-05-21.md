# 2026-05-21

The architecture works. "Residual streams across time" — a single residual block reused across timesteps, with causal attention over its own past states — trains and produces language. That's the headline. Everything else today is detail about what makes it work and what doesn't yet discriminate.

## Residual stream across time: viable, norm-dependent

Three variants of the same core idea at d_model=192, 50K chars TinyShakespeare, 3 epochs. One residual block, temporal attention window k=4:

```json
// experiments/residual_stream_time/artifacts/scaleup-192/training_history.json (no norm)
{"epoch": 3, "val_loss": 3.28991, "val_accuracy": 0.152073}

// experiments/residual_stream_time/artifacts/scaleup-192-timestep-norm/training_history.json (LayerNorm)
{"epoch": 3, "val_loss": 1.787978, "val_accuracy": 0.470008}

// experiments/residual_stream_time_mixadd/artifacts/scaleup-192/training_history.json (mix-add, no LN)
{"epoch": 3, "val_loss": 1.790906, "val_accuracy": 0.46875}
```

| Variant | Val loss | Params | Stream RMS | What happens |
|---------|----------|--------|-----------|--------------|
| [No norm control](../../experiments/residual_stream_time/artifacts/scaleup-192/) | 3.29 | 480K | 1555 | Broken — stream explodes, attention uniform |
| [Temporal pre-norm (LayerNorm)](../../experiments/residual_stream_time/artifacts/scaleup-192-timestep-norm/) | 1.788 | 481K | 3.33 | Recognizable English |
| [Learned mix-add](../../experiments/residual_stream_time_mixadd/artifacts/scaleup-192/) | 1.791 | 479K | 1.01 | Matches LN quality, no normalization layers |

For reference: transformer anchor 1.63 at 186K params (from yesterday's standardized runs). The residual-stream-time models are ~2.6× larger but undertrained — only 3 epochs on 50K chars, loss still declining at termination.

**Temporal norm management is non-negotiable.** Without it the residual stream magnitude grows without bound across timesteps. At RMS 1555, attention scores are saturated and the model outputs near-uniform distributions. With either LayerNorm or mix-add keeping the stream bounded, the same architecture drops 1.5 nats.

**Mix-add matches LayerNorm with no normalization layers.** The mechanism is a learned scalar convex combination at each injection point: `stream = α·stream + (1-α)·new_value`. Learned coefficients after training (from [model_summary.json](../../experiments/residual_stream_time_mixadd/artifacts/scaleup-192/model_summary.json)): token entry α≈0.49, block update α≈0.88, temporal update α≈0.93. The model learns to weight the existing stream heavily at the temporal injection point — cautious about overwriting with temporal context.

Sample output (mix-add, epoch 3):

```text
First Citizen:
Before we proceed any further, hear me speak.

All:
No me the wart he hear the here he hears the wart he hear...
```

Repetitive, but structurally correct (speaker tags, newlines, word boundaries). This is 3 epochs on 50K characters — far from converged.

## Diagonal coupling: inconclusive

Max's "main thing I want to explore" — block i at time T feeds block i+1 at time T+1, creating a diagonal information path through depth×time. Tested with 2 blocks:

```json
// experiments/residual_stream_time_diagonal/artifacts/scaleup-192/model_summary.json
{"parameter_count": 777275, "num_blocks": 2}

// training_history.json
{"epoch": 3, "val_loss": 1.787626, "val_accuracy": 0.476852}
```

| Model | Params | Val loss |
|-------|--------|----------|
| [Single block (LN)](../../experiments/residual_stream_time/artifacts/scaleup-192-timestep-norm/) | 481K | 1.788 |
| [Diagonal 2-block](../../experiments/residual_stream_time_diagonal/artifacts/scaleup-192/) | 777K | 1.788 |

Identical val loss at 1.6× the parameters. This is not a negative result — it's inconclusive. Possible explanations: the scale is too small for depth routing to matter, the training budget is too short to discriminate, or 2 blocks isn't enough to exploit diagonal coupling. The important thing is that diagonal coupling doesn't *hurt*, so it remains a live hypothesis at larger scale.

## Theory-first architecture report

The [residual-stream-across-time design report](../questions/residual-stream-across-time/README.md) maps the full design space: temporal coupling mechanisms (attention vs exponential decay vs gating), residual topologies (shared-stream vs per-block), gradient coupling (full backprop vs local), async semantics, and broadcast scope. Written theory-first with thinker review per the new process from today's dictation, then the minimal probe was derived from it and executed.

## Process changes from dictations

Two dictations today. Key updates incorporated:

1. **Theory-first with thinker review** — new experiments require a written design document reviewed by thinker before any code runs. Prevents "code first, think later."
2. **Multi-workstream support** — the process now accommodates parallel experiment threads.
3. **Mix-add preference** — Max's preferred norm control mechanism. Confirmed working.
4. **Dictations update docs synchronously but don't trigger immediate experiment changes.** New experiments from dictations go into the plan as future work, not interruptions.

## What changed in understanding

The architecture IS viable. Yesterday's narrative ended with "the direction is crystallizing: RNN-based architecture with async shared-memory execution model." Today that direction got its first concrete instantiation and it works. The residual-stream-across-time framing is more specific than "RNN + attention over history" — it's literally one transformer block that persists its residual state and attends causally over its own past.

The critical insight: norm management is the entire difference between "works" and "doesn't work at all." Not a tuning knob — a binary gate. And mix-add (Max's preferred approach) solves it without introducing normalization layers.

## Extended training: gap nearly closed

The mix-add probe was pushed to 100k chars / 5 epochs — the full TinyShakespeare training corpus. Breakthrough result:

```json
// experiments/residual_stream_time_mixadd/artifacts/extended-100k-5ep/tiny_metrics.json
{"parameter_count": 479872, "final_val_loss": 1.670509, "final_val_accuracy": 0.507123}
```

| Run | Val loss | Params | Gap to transformer |
|-----|----------|--------|--------------------|
| Transformer anchor | 1.632 | 186K | — |
| Mix-add short (50k/3ep) | 1.791 | 479K | 0.159 |
| **Mix-add extended (100k/5ep)** | **1.670** | 479K | **0.038** |

Loss trajectory: 1.98 → 1.83 → 1.76 → 1.72 → 1.67 — still declining at epoch 5. One training instability at epoch 4 (loss spike to 51.02, recovered). The 0.038 nat gap to the transformer at 2.6× the params means the architecture is **qualitatively viable** — with enough training it approaches transformer quality.

## Stop-gradient across time: large cost, real speed

The key async prerequisite: what happens if we break temporal gradient flow? Detached stream between every timestep (same architecture, same config).

```json
// experiments/residual_stream_time_stopgrad/artifacts/extended-100k-5ep/tiny_metrics.json
{"parameter_count": 479872, "final_val_loss": 1.893501, "final_val_accuracy": 0.437751, "runtime_seconds": 257.26}
```

| Condition | Val loss | Runtime | Speed vs full grad |
|-----------|----------|---------|-------------------|
| Full gradient | 1.670 | 606s | 1.0× |
| **Full stop-grad** | **1.894** | **257s** | **2.36×** |

Cost: **+0.223 nats**. The gradient signal across time IS doing real work — this isn't free. But training is 2.36× faster because backprop-through-time is eliminated. This directly quantifies the async tradeoff: you can pipeline for speed, but quality degrades substantially without compensation.

**Partial detach sweep in progress** — testing N=2, N=4, N=8, N=16 (detach every N steps). This maps the quality-speed curve between the two endpoints.

## Partial detach: N=4 is the sweet spot

Swept detach-every-N for N=2, 4, 8, 16 — mapping the quality-speed tradeoff curve between full gradient and full stop-grad:

```json
// experiments/residual_stream_time_partial_detach/artifacts/sweep/sweep_summary.json (key excerpt)
{"detach_values": [2, 4, 8, 16]}
```

| Condition | Val loss | Δ from baseline | Runtime | Speed |
|-----------|----------|----------------|---------|-------|
| Full gradient | 1.670 | — | 606s | 1.0× |
| N=16 | 1.693 | +0.023 | 321s | 1.89× |
| N=8 | 1.704 | +0.034 | 286s | 2.12× |
| **N=4** | **1.711** | **+0.041** | **246s** | **2.46×** |
| N=2 | 1.762 | +0.092 | 254s | 2.39× |
| Full stop-grad | 1.894 | +0.223 | 257s | 2.36× |

The curve has a sharp knee at N=4: going from N=1→N=2 recovers 0.131 nats, N=2→N=4 another 0.051, but N=4→N=8 only 0.007. Most gradient signal flows through just 4 timesteps — matching the attention window (k=4). This is probably not coincidence: the attention mechanism IS the pathway that benefits from gradient flow.

**Implication:** async pipelining with 4-step gradient windows gets 2.46× training speed at only +0.041 nats quality cost. The async tradeoff is nearly free with the right truncation window.

## Parameter-matched comparison: moderate efficiency gap

At d_model=116 (184K params ≈ transformer's 186K):

```json
// experiments/residual_stream_time_partial_detach/artifacts/param_matched/param_matched_result.json
{"d_model": 116, "parameter_count": 184156, "final_val_loss": 1.716676}
```

| Model | Params | Val loss |
|-------|--------|----------|
| Transformer anchor | 186K | 1.632 |
| **Residual-stream-time (matched)** | **184K** | **1.717** |
| Residual-stream-time (large) | 479K | 1.670 |

The architecture is **+0.085 nats worse** at matched parameters — a moderate but real efficiency gap. The large model's 1.670 result is mostly benefiting from excess capacity rather than architectural superiority. The gap is likely from: limited temporal context (k=4 vs full sequence), sequential processing, and mix-add bounding.

## What's not established

- Whether larger temporal window (k=8, k=16) closes the efficiency gap. Ablation running.
- Whether the architecture converges further with more epochs (currently still improving at epoch 5).
- Whether multi-block pipelining provides actual inference throughput benefit.

## Next discriminating steps

1. **Window ablation results** — does k=8 or k=16 improve quality? If so, the "k=4 sweet spot" for partial detach might need revisiting.
2. Decide whether to pursue multi-block pipelining (actual async demo) or move to a different experiment family.
