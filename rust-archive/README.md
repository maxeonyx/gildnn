# rust-archive/

Prior Rust implementation — archived. Not the active direction.

The Python rewrite uses the same high-level structure (core/, base-experiments/, experiments/) but starts fresh. These files are kept as reference only. Do not integrate or build on them.
