# Autoregressive MNIST (Pixel Transformer)

We reset this experiment to focus on a single, tiny decoder-only transformer that models rasterised, downsampled MNIST pixels. The goal is to establish one clean baseline that we can trust before introducing patches or flexible sampling orders in follow-up experiments.

## Guided Walkthrough

This report is intentionally paced so new contributors can follow the entire pipeline without jumping into the source code.

### 1. Peek at the Dataset

We work with MNIST digits downsampled to $14\\times14$ to keep sequences short. Each panel should show both the greyscale digit and its histogram of pixel values.

<!-- OUTPUTSLOT:dataset-examples start -->
_Autogenerated dataset examples will appear here (train/test splits, first few digits, value histograms)._
<!-- OUTPUTSLOT:dataset-examples end -->

### 2. Build One Training Example

We flatten the image in raster order, prepend a single start token, and shift the sequence to produce `(tokens[:-1], tokens[1:])` pairs. The table below illustrates the first few positions.

<!-- OUTPUTSLOT:token-trace start -->
_Token trace placeholder — show pixel indices, input token, target token, and decoded pixel value._
<!-- OUTPUTSLOT:token-trace end -->

### 3. Transformer Checkpoints

The simplified model uses:

- Embedding width 64, feed-forward width 128
- 3 decoder blocks, 4 attention heads
- Learned positional embeddings sized to the downsampled grid

We log both train and test metrics at a steady cadence.

<!-- OUTPUTSLOT:training-metrics start -->
_Training/test loss and bits-per-dim charts will render here once the harness runs._
<!-- OUTPUTSLOT:training-metrics end -->

### 4. Sampling Through Training

Sampling snapshots help sanity-check progress long before final evaluation.

<!-- OUTPUTSLOT:train-snapshots start -->
_Autogenerated panels for early/mid/late training epochs will be inserted here._
<!-- OUTPUTSLOT:train-snapshots end -->

### 5. Final Evaluation

Use a larger test batch (e.g., 256 digits) for the last panel so variance settles and we can compare runs.

<!-- OUTPUTSLOT:final-batch start -->
_Final evaluation metrics and sampled completions appear here after the full run._
<!-- OUTPUTSLOT:final-batch end -->

## Experiment Status

- ✅ Directory reset with lean configuration and report scaffolding.
- ⬜ Implement the downsampled pixel transformer and deterministic training loop.
- ⬜ Regenerate `report.md` and `benchmark.json` directly from the harness.

If the scope ever starts to balloon (multiple architectures, extra priors, etc.), pause and split the work into follow-up experiments:

- `base-experiments/autoregressive-patch-mnist` for discrete-VAEs and patch tokens.
- `base-experiments/autoregressive-mnist-sampling-orders` for order-agnostic conditioning and sampling tricks.

Please push back whenever new requests combine several of these directions; we should land one validated step at a time.
